---
# PB-437 · finalization_gate · DRAFT (BahyWay.Ecosystem v4.0 finalization suite)
# Running this playbook is the Architect's CSR-08 confirmation. Nothing is law until run.
- name: "PB-437 · v4.0 finalization gate: assert PB-427..436 sealed in NARU before monitoring build begins"
  hosts: uruk
  tasks:
    - name: assert the chain is sealed
      ansible.builtin.shell: "grep -c 'PB-42[7-9]\\|PB-43[0-6]' {{ playbook_dir }}/../workspace/bahyway_v4/NARU.journal"
      register: sealed
      failed_when: sealed.stdout|int < 10
    - name: journal (NARU discipline)
      ansible.builtin.lineinfile:
        path: "{{ playbook_dir }}/../workspace/bahyway_v4/NARU.journal"
        line: "{{ ansible_date_time.iso8601 }} · PB-437 · finalization gate passed · monitoring build authorized"
        create: yes
