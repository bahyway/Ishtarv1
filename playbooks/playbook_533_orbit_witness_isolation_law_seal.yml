---
# ============================================================================
# PB-185 — Orbit Witness & Isolation Law: Concept Seal (GL-VIZ-002)
# BahyWay.Ecosystem v4.0 — DubSar Theater (Godot 4.7, witness renderer)
# ----------------------------------------------------------------------------
# PURPOSE:
#   Seals GL-VIZ-002, the interaction law governing particle picking,
#   orbit-scope selection, and orbit isolation (Hubble Mode) on the
#   BIGRING stage. Ratified by the Architect as its OWN law (not a
#   clause of GL-VIZ-001), with the isolated view ratified as an
#   OPERATING MODE of the existing ORBIT 3D lens (GL-DST-001) —
#   the Theater keeps its seven lenses.
#   CONCEPT DOCUMENT ONLY — implementation queued behind PB-160
#   per the Governing Law.
#
# GUARDED BOUNDARY:
#   Selection is witnessing. It EMITs; it never mutates. Theater
#   remains the stage — GeoEngine remains the single truth source.
#
# SOVEREIGNTY:
#   - Run by the Architect (DUB.SAR 𒁾) from host → eriduous-vdi (CSR-08).
#
# TARGET: eriduous-vdi (192.168.122.214) — sovereign law catalog node
#
# CHANGED during Phase 2 integration (2026-08-15): originally deployed
# GL-VIZ-002 by an Ansible `copy:` task to a remote $HOME path (and, in
# some cases, was the ONLY place this document's text existed at all --
# confirmed by checking docs/ before this fix). Per the PB-197+
# convention (sealed docs are committed directly into docs/, not
# Ansible-copied to a machine), the document now lives at
# docs/09_observatory/GL-VIZ-002-orbit-witness-isolation.md and this playbook is a pointer, matching
# playbook_301's own fix. Renumbered from PB-185 to PB-533 during
# the same integration -- see docs/phase2-incoming/RENUMBERING_MAP.md.
# ============================================================================

- name: "PB-533 | Record — Orbit Witness & Isolation Law (GL-VIZ-002) landed"
  hosts: localhost
  connection: local
  become: false
  tasks:

    - name: "PB-533.1 | Pointer to the actual document"
      ansible.builtin.debug:
        msg: >-
          GL-VIZ-002 — Orbit Witness & Isolation Law — is committed at docs/09_observatory/GL-VIZ-002-orbit-witness-isolation.md.
          Documentation only -- no Rust code changed by this playbook.
