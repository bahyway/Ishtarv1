---
# PB-516 · vineyard_harvest_rules · DRAFT (BahyWay.Ecosystem v4.0 · traffic & HeptaMapSpace arc)
# Running this playbook is the Architect's CSR-08 confirmation.
- name: "PB-516 · harvest rules engine: ripe≥85%∧age≥12 → snapshot INTO EnkiDW (vintage SN-xxx)"
  hosts: enkidb-node-write
  tasks:
    - name: rules
      ansible.builtin.debug: {msg: "harvest · green-harvest (infection>covenant) · red→sorting table (EnkiQDB, containment)"}
    - name: journal (NARU)
      ansible.builtin.lineinfile:
        path: /home/bfadam/Forge/bahyway_v4/NARU.journal
        line: "{{ ansible_date_time.iso8601 }} · PB-516 · harvest rules engine scaffolded"
        create: yes
