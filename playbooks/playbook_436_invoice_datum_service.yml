---
# PB-436 · invoice_datum_service · DRAFT (BahyWay.Ecosystem v4.0 finalization suite)
# Running this playbook is the Architect's CSR-08 confirmation. Nothing is law until run.
- name: "PB-436 · self-explaining invoice itemizer: per-file ops/€ traced to residence and cause"
  hosts: uruk
  tasks:
    - name: service scaffold
      ansible.builtin.command:
        cmd: "cargo new --bin bahyway-invoiced"
        creates: /home/bfadam/Forge/bahyway_v4/bahyway-invoiced/Cargo.toml
    - name: clause
      ansible.builtin.debug: {msg: "every euro traceable to a file, a residence, and a cause — the client sees the residue before the invoice does (SLA Self-Explaining Invoice clause)"}
    - name: journal (NARU discipline)
      ansible.builtin.lineinfile:
        path: /home/bfadam/Forge/bahyway_v4/NARU.journal
        line: "{{ ansible_date_time.iso8601 }} · PB-436 · invoiced scaffolded"
        create: yes
