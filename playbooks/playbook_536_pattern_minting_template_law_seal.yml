---
# ============================================================================
# PB-188 — Pattern Minting & Template Law: Concept Seal (GL-TPL-001)
# BahyWay.Ecosystem v4.0 — CompareEngine · TemplateEngine · EnkiMDB Arsenal
# ----------------------------------------------------------------------------
# PURPOSE:
#   Seals GL-TPL-001, the law by which unrecognized particle behavior
#   becomes a new sealed pattern, and a pattern becomes a reusable
#   .tmpl artifact with full KAKI v4.0 identity. Governs the two-
#   witness matching verdict (Jaccard on story shingles + Nabû
#   metric in Hepta Space), the MinHash scale clause, the client-
#   abstraction IP clause, the steward minting rite, and the
#   template-as-particle registration in EnkiMDB.
#   NAMING: CompareEngine stands as ratified. TemplateEngine's
#   god-name is PENDING the Architect's NL-001 ruling — Kulla
#   (god of bricks and the brick-mold) is proposed.
#   CONCEPT DOCUMENT ONLY — implementation queued behind PB-160.
#
# GUARDED BOUNDARY:
#   All similarity computation lives in CompareEngine (Rust, behind
#   HEPT). DubSar Theater selects scopes and witnesses scores; it
#   never computes them. Templates capture SHAPE, never client data.
#
# SOVEREIGNTY:
#   - Run by the Architect (DUB.SAR 𒁾) from host → eriduous-vdi (CSR-08).
#
# TARGET: eriduous-vdi (192.168.122.214) — sovereign law catalog node
#
# CHANGED during Phase 2 integration (2026-08-15): originally deployed
# GL-TPL-001 by an Ansible `copy:` task to a remote $HOME path (and, in
# some cases, was the ONLY place this document's text existed at all --
# confirmed by checking docs/ before this fix). Per the PB-197+
# convention (sealed docs are committed directly into docs/, not
# Ansible-copied to a machine), the document now lives at
# docs/06_governance_parzu/GL-TPL-001-pattern-minting-template.md and this playbook is a pointer, matching
# playbook_301's own fix. Renumbered from PB-188 to PB-536 during
# the same integration -- see docs/phase2-incoming/RENUMBERING_MAP.md.
# ============================================================================

- name: "PB-536 | Record — Pattern Minting & Template Law (GL-TPL-001) landed"
  hosts: localhost
  connection: local
  become: false
  tasks:

    - name: "PB-536.1 | Pointer to the actual document"
      ansible.builtin.debug:
        msg: >-
          GL-TPL-001 — Pattern Minting & Template Law — is committed at docs/06_governance_parzu/GL-TPL-001-pattern-minting-template.md.
          Documentation only -- no Rust code changed by this playbook.
