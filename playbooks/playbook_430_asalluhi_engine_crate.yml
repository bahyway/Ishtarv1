---
# PB-430 · asalluhi_engine_crate · DRAFT (BahyWay.Ecosystem v4.0 finalization suite)
# Running this playbook is the Architect's CSR-08 confirmation. Nothing is law until run.
- name: "PB-430 · scaffold asalluhi-core (GL-FOR-001): Āšipu + Asû calculi, R-xx/C-xx registries"
  hosts: uruk
  tasks:
    - name: ensure Forge workspace root
      ansible.builtin.file: { path: "{{ playbook_dir }}/../workspace/bahyway_v4", state: directory, mode: "0755" }
      check_mode: false
    - name: cargo new
      ansible.builtin.command:
        cmd: "cargo new --lib asalluhi-core"
        creates: "{{ playbook_dir }}/../workspace/bahyway_v4/crates/asalluhi-core/Cargo.toml"
        chdir: "{{ playbook_dir }}/../workspace/bahyway_v4/crates"
    - name: registries
      ansible.builtin.copy:
        dest: "{{ playbook_dir }}/../workspace/bahyway_v4/crates/asalluhi-core/src/registry.rs"
        content: |
          //! Symptom registry (Asû): R-01 CHECKSUM_DRIFT · R-02 EAV_MANDATORY_MISSING · R-03 HUE_VARIANCE · R-04 RIKSU_ORPHAN · R-05 STALE_TEMPORAL · FMT-01 FORMAT_REFUSAL
          //! Cause registry (Āšipu): C-01 SOURCE_FEED_POISON · C-02 TEMPLATE_DEFECT · C-03 PASHIRU_CALIBRATION · C-04 RIKSU_SEVERANCE · C-05 RENEWAL_RITE_HALTED
          //! Law: DETECT → PROVE → PREDICT → PRESCRIBE — no cure before cause; two witnesses always.
    - name: journal (NARU discipline)
      ansible.builtin.lineinfile:
        path: "{{ playbook_dir }}/../workspace/bahyway_v4/NARU.journal"
        line: "{{ ansible_date_time.iso8601 }} · PB-430 · asalluhi-core scaffolded"
        create: yes
