---
# PB-508 · shakkanakku_chronicle_sink · DRAFT (BahyWay.Ecosystem v4.0 finalization suite)
# Running this playbook is the Architect's CSR-08 confirmation. Nothing is law until run.
- name: "PB-508 · chronicle sink: every bell, drill, decree, and rite appended — the Watch remembers"
  hosts: uruk
  tasks:
    - name: chronicle law
      ansible.builtin.debug: {msg: "append-only · the expected silence of the Δ-bell is itself recorded as testimony"}
    - name: journal (NARU discipline)
      ansible.builtin.lineinfile:
        path: /home/bfadam/Forge/bahyway_v4/NARU.journal
        line: "{{ ansible_date_time.iso8601 }} · PB-508 · chronicle sink installed"
        create: yes
