---
# PB-510 · rite_lamassu_sweep · DRAFT (BahyWay.Ecosystem v4.0 finalization suite)
# Running this playbook is the Architect's CSR-08 confirmation. Nothing is law until run.
- name: "PB-510 · standing rite: Lamassu cadence sweep · every 2.2 s · β sampled, never per-particle"
  hosts: enkidb-node-write
  tasks:
    - name: systemd timer
      ansible.builtin.debug: {msg: "lamassu-cadenced.timer OnUnitActiveSec=2.2s (installed by PB-502; this rite blesses & verifies it)"}
    - name: journal (NARU discipline)
      ansible.builtin.lineinfile:
        path: /home/bfadam/Forge/bahyway_v4/NARU.journal
        line: "{{ ansible_date_time.iso8601 }} · PB-510 · sweep rite blessed"
        create: yes
