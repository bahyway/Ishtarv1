{
  "name": "golden suite v1 · the cases whose correct verdict is already known",
  "sealed": true,
  "note": "the watcher may not add, remove or edit a case (GL-AGT-001 §5)",
  "cases": [
    {"id":"G-1","kind":"regression","candidate":"widen Kaki to 24 bytes",
     "intent":["none"],
     "expect":{"verdict":"REFUSE","because":"identity of every stored particle becomes UNKNOWN"}},
    {"id":"G-2","kind":"regression","candidate":"cluster on uuid_hash",
     "intent":["lookup"],
     "expect":{"verdict":"REFUSE","because":"orbit locality worsens far beyond the threshold; undeclared"}},
    {"id":"G-3","kind":"regression","candidate":"sample 1-in-16 in the field pass",
     "intent":["speed"],
     "expect":{"verdict":"REFUSE","because":"beta1 falls because holes stopped being sampled, not because they closed"}},
    {"id":"G-4","kind":"regression","candidate":"relax a gate to one witness",
     "intent":["backlog"],
     "expect":{"verdict":"REFUSE","because":"tau rises: suspicions are admitted as verdicts"}},
    {"id":"G-5","kind":"clean","candidate":"inline the grid index",
     "intent":["speed"],
     "expect":{"verdict":"PROMOTE","because":"no shape moves at any scale"}},
    {"id":"G-6","kind":"unreadable","candidate":"a tree with an unreadable extent map",
     "intent":["none"],
     "produces_unknown": true,
     "expect":{"verdict":"UNKNOWN_MUST_APPEAR",
               "because":"a silent success here would be the dangerous outcome (§4)"}},
    {"id":"G-7","kind":"determinism","candidate":"the same tree twice",
     "intent":["none"],
     "expect":{"verdict":"IDENTICAL_RECEIPT","because":"same inputs, same sha256 (§1)"}}
  ]
}
