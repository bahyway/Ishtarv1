---
# PB-525 · hotspot_detection · DRAFT (BahyWay.Ecosystem v4.0 · traffic & HeptaMapSpace arc)
# Running this playbook is the Architect's CSR-08 confirmation.
- name: "PB-525 · incident hotspot detection: DBSCAN + Getis-Ord Gi* + TDA structural anomaly (noise/crash risk surface)"
  hosts: enkidb-node-write
  tasks:
    - name: honesty
      ansible.builtin.debug: {msg: "risk surface (where), not point-prediction (when) · TDA flags patterns a heatmap misses"}
    - name: journal (NARU)
      ansible.builtin.lineinfile:
        path: /home/bfadam/Forge/bahyway_v4/NARU.journal
        line: "{{ ansible_date_time.iso8601 }} · PB-525 · hotspot detection scaffolded"
        create: yes
