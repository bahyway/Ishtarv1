---
# PB-509 · watch_ui_deploy · DRAFT (BahyWay.Ecosystem v4.0 finalization suite)
# Running this playbook is the Architect's CSR-08 confirmation. Nothing is law until run.
#
# 2026-08-24: the SOURCE tablet drafts live in the Architect's own personal
# research area (Forge/bahyway_v4, a separate repo -- NOT this one), not in
# EnkiDB. Only the sealed OUTPUT (shala_vault) belongs inside this repo's
# workspace/bahyway_v4. Override dialyworks_root with -e if your drafts
# live somewhere else.
- name: "PB-509 · deploy the Watch UI: Šala tablet now, ws://:9100/watch feed reserved for the Theater panel"
  hosts: uruk
  vars:
    dialyworks_root: /home/bfadam/Forge/bahyway_v4/docs/__DialyWorks
  tasks:
    - name: deploy tablet
      ansible.builtin.command:
        cmd: "bash -c 'cp {{ dialyworks_root }}/Tu20260811/shala_igigi_watch_v2.html {{ playbook_dir }}/../workspace/bahyway_v4/shala_vault/'"
    - name: feed contract
      ansible.builtin.debug: {msg: "ws://:9100/watch · {bells, headroom, weir, decay, sentinels} v1 — consumers adapt to it, never it to them"}
    - name: journal (NARU discipline)
      ansible.builtin.lineinfile:
        path: "{{ playbook_dir }}/../workspace/bahyway_v4/NARU.journal"
        line: "{{ ansible_date_time.iso8601 }} · PB-509 · Watch UI deployed · feed contract reserved"
        create: yes
