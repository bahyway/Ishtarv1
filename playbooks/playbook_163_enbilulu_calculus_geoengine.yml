# =============================================================================
# PB-163 — ENBILULU CALCULUS BUILT INTO GEOENGINE (not WPDEngine-only)
# BahyWay.Ecosystem v4.0 — 2026-07-10, corrected same day
#
# CONTEXT: the Architect asked for Enbilulu 𒀭𒂗𒉈𒇻 to be added to "the
# GeoEngine Mathematics Collection AND NOT ONLY WPDEngine." Two things
# had to be corrected first, checked directly against the repo:
#   - "PB-150 in the current sealing sprint covered the WPDEngine /
#     Enbilulu implementation" — FALSE. No playbook_150 exists anywhere
#     in this repository. Same fabrication pattern as CSR-08/PB-152
#     earlier this session.
#   - "GeoEngine" — a standalone crate literally named `geo-engine`
#     did not exist. At least four sealed concept documents
#     (HS-EXT-002's epsilon_geo, GL-MRD-002's Nabu metric, GL-DST-001's
#     stage-vs-truth boundary, TPL-001's index-covered-proof law) cite
#     it by name as an existing authority.
#
# SAME-DAY CORRECTION (this revision): the first pass built a brand new,
# empty `crates/geo-engine` and landed Enbilulu there. The Architect
# then pointed out this repeated a mistake made earlier the same session
# with `enkidb-engine`: the real math home already existed, under the
# name `bahyway-algebra` (BahyWay Unified Algebra) — Clifford algebra,
# Lie algebra, 20 topological operators, HOMT axioms, orbital mechanics
# — with four real dependents already, including `bin/enkidb-query-server`
# itself. `bahyway-algebra` never called itself "GeoEngine" in its own
# code (unlike `enkidb-engine`, which literally says "EnkiDB — main
# database orchestration engine" in its own description), so this
# wasn't as directly provable as that earlier case, but it's the
# architecturally correct home and the Architect confirmed the intent.
# Enbilulu was moved there; the standalone `geo-engine` crate was
# removed entirely rather than left as a duplicate.
#
# WHAT WAS DONE (current, corrected state):
#   - crates/bahyway-algebra/src/enbilulu.rs: the Calculus itself,
#     now a module of the real, pre-existing, load-bearing math crate.
#     bahyway-algebra's own lib.rs now states explicitly that it is the
#     crate the sealed documents mean by "GeoEngine."
#       * Phi_Enbi junction defect potential, weights [0.20, 0.20, 0.15,
#         0.30, 0.15] (confirmed exact by the Architect). baru_residual
#         (kappa, the King-plot residual) is the heaviest-weighted
#         factor (0.30) -- confirmed by name. The other four factors are
#         NOT confirmed by name; placeholders marked TODO(ARCHITECT) in
#         the source, pending BCENV001_Enbilulu_Calculus.md /
#         BCENV001_Enbilulu_WPDEngine_Rev2.md, which are referenced by
#         an earlier session's verification doc
#         (docs/13_changelog/BATCH2_UrNammu_Marduk_Enbilulu_VERIFIED_2026-07-07.md)
#         but are not present in this checkout.
#       * TiamatBand: Stable <40, Watch 40-60, Serious 60-80, ERRA >=80
#         (confirmed exact).
#       * enbi_horizon_weeks(): linear extrapolation to ERRA crossing,
#         given current Phi_Enbi and a weekly rate of change.
#       * expansion_horizon_weeks(): the "T shrinks to T'" recompute
#         under a hypothetical capacity expansion.
#       * Terru diagnosis: STARTER fingerprint table only (5 illustrative
#         entries), explicitly flagged as not sourced from the sealed
#         document -- replace when the real table is available.
#       * narrate(): produces the exact compass sentence, tested
#         verbatim: "Element E, failing by mechanism M, from cause C,
#         crossing ERRA in T weeks -- and if expansion X proceeds, T
#         shrinks to T'."
#       * to_alert_severity(): bridges TIAMAT bands into the real
#         alert-engine's AlertSeverity (Watch/Warning/Critical) so Milu
#         alerts flow through existing alerting machinery rather than a
#         parallel system.
#   - crates/wpd-engine: now DEPENDS on bahyway-algebra (junction.rs is
#     the consumer, not a re-derivation) -- proves the "single truth
#     source" law by actually calling into it.
#   - crates/wpd-engine/src/sector.rs: BaghdadSector::tribe_id() added
#     (was missing -- "Baghdad sectors mapped to KAKI tribe IDs" was
#     also not yet real). Reserved range 0x3000-0x3006, following the
#     precedent already set by EnkiduLLM (0x1001-0x10FF, 0x1200) and
#     ESARHADDON (0x2000-0x20FF).
#
# TESTS: bahyway-algebra 103/103 (89 pre-existing + 14 Enbilulu),
#   wpd-engine 50/50 (including 3 junction tests and 2 sector tribe_id
#   tests), full workspace build clean, including all four crates that
#   already depended on bahyway-algebra before this change.
#
# STILL OPEN:
#   - Factor 1/2/3/5 real names/definitions, and the real Terru
#     fingerprint table, need the Architect's source .md files -- not
#     invented here.
#   - No playbook-level "install" step: this landed as direct commits to
#     the working branch, same as the ESARHADDON/adapa-recall work two
#     turns prior. This file is the record, matching PB-161's pattern.
# =============================================================================
---
- name: "PB-163 | Record — Enbilulu Calculus landed in bahyway-algebra (the real GeoEngine)"
  hosts: localhost
  connection: local
  become: false
  tasks:
    - name: "PB-163.1 | Pointer to the actual change"
      ansible.builtin.debug:
        msg: >-
          See commit history on claude/iphone-playbooks-crosstribe-eval-nbc5sw,
          2026-07-10, for the real diff. Enbilulu lives in
          crates/bahyway-algebra/src/enbilulu.rs; wpd-engine consumes it
          via junction.rs. The standalone geo-engine crate from the first
          pass was removed same-day after the Architect confirmed
          bahyway-algebra was already meant to serve that role. This
          playbook is a record, not an installer.
