---
# PB-431 · hendursaga_theater_feed · DRAFT (BahyWay.Ecosystem v4.0 finalization suite)
# Running this playbook is the Architect's CSR-08 confirmation. Nothing is law until run.
- name: "PB-431 · DubSar Theater adapter consuming the sealed ws://:9000/position contract"
  hosts: uruk
  tasks:
    - name: ensure Forge workspace root
      ansible.builtin.file: { path: "{{ playbook_dir }}/../workspace/bahyway_v4", state: directory, mode: "0755" }
      check_mode: false
    - name: contract note
      ansible.builtin.debug: {msg: "PositionV1 {lat,lng,alt,heading,speed,t} — consumers adapt to the schema, never it to them (GL-NAV-001 Invariant 3)"}
    - name: adapter scaffold
      ansible.builtin.command:
        cmd: "cargo new --lib hendursaga-theater-feed"
        creates: "{{ playbook_dir }}/../workspace/bahyway_v4/crates/hendursaga-theater-feed/Cargo.toml"
        chdir: "{{ playbook_dir }}/../workspace/bahyway_v4/crates"
    - name: journal (NARU discipline)
      ansible.builtin.lineinfile:
        path: "{{ playbook_dir }}/../workspace/bahyway_v4/NARU.journal"
        line: "{{ ansible_date_time.iso8601 }} · PB-431 · Theater feed adapter scaffolded"
        create: yes
