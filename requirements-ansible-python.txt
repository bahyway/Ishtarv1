# Python packages required on the control node/target for the ansible.posix
# and community.docker collections declared in requirements.yml to actually
# run (install with: pip install -r requirements-ansible-python.txt).
# Discovered missing 2026-08-26 running playbook_633/634 (community.docker.
# docker_container) against the current bare-metal host: "Failed to import
# the required Python library (requests)".
docker
requests
