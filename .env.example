# ═══════════════════════════════════════════════════════════════
#  BahyWay.Ecosystem v4.0 — root Environment Variables
#  cp .env.example .env && edit .env with your real values
#  .env itself is gitignored -- never commit real credentials here.
# ═══════════════════════════════════════════════════════════════

# ── Gemini (used by docker-compose.yml's frontend service build) ──
GEMINI_API_KEY=change_me_your_real_gemini_api_key
VITE_GEMINI_API_KEY=change_me_your_real_gemini_api_key
