# GL-FLD-001 · Tablet of the Wave-Field Membrane
**SEALED** · 2026-08-24 · Amsterdam · sealed by DUB.SAR 𒁾
Rite: CSR-08 · Seal: Ed25519 AkkadianSeal, computed by PB-363
Depends on: GL-UNT-001, GL-FED-001, EN-MDB-001 (Masku mask-as-view), ENLIL HotIndex, Never-Averaged Theorem (GL-ALG-001-A2)

---

## §1 Amplitude is density
The membrane's height at `(m, θ, r)` is the **count** of particles scatter-added into that bin by a single ENLIL pass.
It is not a rendering of density; **it is the density**.
Cost scales with the grid, not with N. This is how a BIGRING of 10⁹ becomes one surface within the sub-second law.

## §2 PU per shell
`PU_m` is the σ of shell *m*'s density field — a true integral over the field, answering how much structure that layer carries.
The shells sum to the mixed membrane; where they do not, the remainder is the **cross-term band** and is displayed as its own quantity, **never folded silently into the components**.
Superposition is an assumption. A body is not linear, and a controller is not linear.

## §3 Phase names the cause
Amplitude says a layer is loud; **phase** says which layers are driven together.
Two shells whose dominant angular mode is phase-locked across two epochs share a cause; coincident magnitude alone does not.
The verdict cites the locked shells, their phase, and their RU wavelength.

## §4 Two instruments, never one
The **field** finds the crowd's shape and the residual.
The **ember chamber** finds the rare — a hundred particles in a billion that no average can hold.
Neither may stand alone: *a quiet field with hot embers is not a quiet organism.*

## §5 Field is a view (Masku)
The field is a mask over sovereign particles and is **never the store**.
Descent through a bin re-runs the generator and returns the citizens' KAKIs with their biographies.
Identity must be recoverable at every point of the surface, or the surface is not admissible.

## §6 Ambiguous is not incoherent
`AMBIGUOUS` — a reading on a band edge: graded membership, geometry sound.
`INCOHERENT` — a layer whose phase scatters between epochs: **verdict withheld**.
COHERENCE is its own epistemic class beside MEASURED / DERIVED / ESTIMATED / ADVISED.
Incoherence must never borrow the credibility of ambiguity.

## §7 Residual across organisms
Two point clouds of 10⁹ have no correspondence and cannot be compared. **Two fields subtract.**
The residual against the federation median is the abnormality, at every RU and every shell at once, for the price of one subtraction.

## §8 The court freezes on catch
A moving membrane cannot be worked. On descent or catch, advection and auto-orbit stop and the stage holds still; the held particle bounces in place (Ṣabātu) so the court always knows which one it has.
Freezing changes only the view — field, epoch and verdict are untouched.

## §9 A caught particle is never shown alone
Catching lights the relation web **by default**: `siblings` (bin cohort), `ring` (its RU contour in its shell), `spoke` (its θ column across radius), and `column` (the same address read through all seven MLU shells).
The StoryEngine is a **named rite** in the sovereign menu, not a side effect of clicking.

## §10 The biography is a shape (Anti-SQL)
A particle's account is delivered as a **simplicial complex**, not a result set.
Vertices are its events and cohorts; a relation binding three or more is a filled 2-simplex; a column spanning three shells is a 3-simplex.
`β₀` says how many disconnected stories the account has. `β₁` counts loops surviving after known relations are filled — **an unfilled loop is an account with a hole in it**, and points at a law not yet written.
Every line carries its epistemic class; `τ` is the share of the account that is not MEASURED.
The HeptaScript that produced it is shown beside it. **No SELECT, no FROM, no WHERE-clause, no JOIN — ever.**

## §11 Amendment
Amendments `GL-FLD-001-A1…`. §4 and §5 may not be amended: they are the Never-Averaged and Masku guarantees of this tablet.
