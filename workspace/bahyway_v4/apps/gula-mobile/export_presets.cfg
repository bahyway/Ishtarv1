[preset.0]
name="Android"
platform="Android"
runnable=true
; Fill keystore + SDK paths on the host before export.
; Sovereignty note: no Google Play services dependency; maps are
; bundled offline tiles (PB-324), advisory via PB-321 endpoint.
