---
- name: "𒁾 Playbook 20 — applied via direct script on 2026-06-18"
  hosts: localhost
  connection: local
  become: false
  tasks:
    - name: "Run fix script"
      ansible.builtin.command:
        cmd: python3 /tmp/bw_fix_20.py
      register: out
      changed_when: true
    - name: "Build"
      ansible.builtin.command:
        cmd: cargo build -p dubsar-visualizer
        chdir: /home/bahyway/Forge/bahyway_v4
      changed_when: true
