[package]
name              = "bahyway-api"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "BahyWay Live Data API — serves live_export.json over HTTP with CORS (pure std, no external crates)"

[[bin]]
name = "bahyway-api"
path = "src/main.rs"

[dependencies]
bahyway-core = { path = "../../crates/bahyway-core" }
