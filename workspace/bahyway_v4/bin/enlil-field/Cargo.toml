[package]
name              = "enlil-field"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "enlil-field -- real GL-FLD-001 wave-field kernel: scatter-adds every real particle in the local store into a shells x angles x radii grid and proves conservation, Masku descent, and the sub-second scatter-add law"

[[bin]]
name = "enlil-field"
path = "src/main.rs"

[dependencies]
bahyway-algebra       = { path = "../../crates/bahyway-algebra" }
enkidb-particle-store = { path = "../../crates/enkidb-particle-store" }
toml                  = "0.8"
