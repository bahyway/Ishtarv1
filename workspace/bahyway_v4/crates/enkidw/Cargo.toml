[package]
name              = "enkidw"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "EnkiDB Data Warehouse — ETL pipeline, LandingZone, ZipEngine, WayCompiler, DW analytics (§12)"

[dependencies]
bahyway-core      = { path = "../bahyway-core" }
bahyway-crc       = { path = "../bahyway-crc" }
enkidb-kaki       = { path = "../enkidb-kaki" }
enkidb-journal    = { path = "../enkidb-journal" }
enkidb-storage    = { path = "../enkidb-storage" }
enkidb-engine     = { path = "../enkidb-engine" }
enkidb-persist    = { path = "../enkidb-persist" }
story-engine      = { path = "../story-engine" }
aaol              = { path = "../aaol" }
musaru-security       = { path = "../musaru-security" }
compare-tribe-schema  = { path = "../compare-tribe-schema" }
media-type-detector   = { path = "../media-type-detector" }
# Shape-signature analytics (dw_analytics::report_with_shape /
# shape_trajectory): wires the existing TDA sentinel into EnkiDW's own
# report instead of duplicating persistent-homology math here.
lamassu-engine        = { path = "../lamassu-engine" }
