[package]
name              = "massartu-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Massartu — the domain-neutral DETECT -> PROVE -> PREDICT -> PRESCRIBE watch pattern (PH-002 Corollary 1: nucleus-exchange invariance)"

[dependencies]
trend-core = { path = "../trend-core" }

[dev-dependencies]
fdd-core   = { path = "../fdd-core" }
egd-engine = { path = "../egd-engine" }
