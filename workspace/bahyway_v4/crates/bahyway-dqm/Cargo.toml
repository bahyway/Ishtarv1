[package]
name        = "bahyway-dqm"
version     = { workspace = true }
edition     = { workspace = true }
authors     = { workspace = true }
license     = { workspace = true }
description = "BahyWay Data Quality Metrics — 6 industry dimensions with sovereign algorithms (Levenshtein, Jaro-Winkler, Soundex, Z-score, Merkle, rule engine)"

[dependencies]
bahyway-core = { path = "../bahyway-core" }
bahyway-crc  = { path = "../bahyway-crc" }
