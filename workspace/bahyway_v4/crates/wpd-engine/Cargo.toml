[package]
name        = "wpd-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "𒀭𒍪 WPD sovereign pipeline defect detection — BaghdadSector×7, 12-band spectral. Junction defect potential (Enbilulu Calculus) lives in bahyway-algebra (the real GeoEngine), consumed here rather than duplicated."

[dependencies]
nusku-engine    = { path = "../nusku-engine" }
bahyway-algebra = { path = "../bahyway-algebra" }
alert-engine    = { path = "../alert-engine" }
