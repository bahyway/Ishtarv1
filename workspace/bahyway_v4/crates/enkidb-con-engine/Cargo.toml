[package]
name        = "enkidb-con-engine"
version     = "0.1.0"
edition     = "2021"
description = "Sovereign Connection Engine — 7 CSR security rules, no third-party deps"

[dependencies]
# Pure sovereign Rust — no external crates
bahyway-crc = { path = "../bahyway-crc" }
