[package]
name              = "enkidb-session-registry"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Sovereign session registry — loads enkidb-sessions.toml, sync, no third-party deps"

[dependencies]
# Pure sovereign Rust — no external crates
