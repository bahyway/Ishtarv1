[package]
name              = "elu-tribe-hotindex"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Elu Index Stack hot tribe-resolution layer -- L1/L2-cache-resident O(1) projection of the NUZI registry, plus a branchless billion-particle batch-sweep kernel (from the iPhone-session PB-152 review, renamed from ENLIL to avoid colliding with the real Enlil stability algebra; renamed again from Anu to Elu, 2026-08-15, when the Architect reclaimed Anu for the AnuGovernor rename)"

[dependencies]

[[bin]]
name = "bench_sweep"
path = "src/bin/bench_sweep.rs"
