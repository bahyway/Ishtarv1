[package]
name              = "pdm-discovery"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "v1 relationship-heuristic complex-builder for DubSar PDM schema discovery: exact-match join-key detection across uploaded tabular data, feeding bahyway-algebra::persistence's clique_complex_persistence to surface a real discovered-schema proposal (entities, candidate relationships, integrity voids)."

[dependencies]
bahyway-algebra = { path = "../bahyway-algebra" }
