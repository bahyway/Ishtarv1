[package]
name              = "plancache-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Sealed, compile-once query execution plans + journal projection cache (from the iPhone-session PB-158/159 review, TPL-001 sections A/B), corrected: renamed Template->SealedPlan to avoid colliding with the real template-engine::Template, and the admission law no longer hard-depends on the not-yet-wired HeptaShellIndex"

[dependencies]
