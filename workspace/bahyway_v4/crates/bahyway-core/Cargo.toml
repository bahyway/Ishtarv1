[package]
name              = "bahyway-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Core types, errors, traits used across all crates"

[dependencies]
bahyway-crc = { path = "../bahyway-crc" }
