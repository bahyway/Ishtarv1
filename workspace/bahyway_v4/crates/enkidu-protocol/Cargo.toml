[package]
name              = "enkidu-protocol"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "TCP wire protocol for federated HeptaScript queries — SPSC ring buffer, buffer pool, zero-copy streaming"

[dependencies]
enkidb-kaki = { path = "../enkidb-kaki" }

[dev-dependencies]
