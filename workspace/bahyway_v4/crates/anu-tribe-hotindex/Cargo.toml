[package]
name              = "anu-tribe-hotindex"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Anu Index Stack hot tribe-resolution layer -- L1/L2-cache-resident O(1) projection of the NUZI registry, plus a branchless billion-particle batch-sweep kernel (from the iPhone-session PB-152 review, renamed from ENLIL to avoid colliding with the real Enlil stability algebra and this repo's own Anu Index Stack rename)"

[dependencies]

[[bin]]
name = "bench_sweep"
path = "src/bin/bench_sweep.rs"
