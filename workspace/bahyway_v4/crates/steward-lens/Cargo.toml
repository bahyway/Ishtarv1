[package]
name              = "steward-lens"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "StewardLens — read-only sovereign auditor for DataSteward oversight across all 4 device tiers"

[dependencies]
enkidb-kaki = { path = "../enkidb-kaki" }
bahyway-core = { path = "../bahyway-core" }
