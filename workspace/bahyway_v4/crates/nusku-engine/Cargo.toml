[package]
name = "nusku-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "𒀭𒉪 Nusku sovereign shared types — KakiPK, Hepta, ParticleSignal, BodyScan, pipeline"

[dependencies]
# Pure Rust — zero external dependencies
