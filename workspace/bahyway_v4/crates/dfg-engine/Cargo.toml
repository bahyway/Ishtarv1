[package]
name              = "dfg-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "DFG (Data Flow Graph) execution model — nodes, edges, governance decisions (UEK §3, §7, §10)"

[dependencies]
# Pure sovereign Rust — no external crates
