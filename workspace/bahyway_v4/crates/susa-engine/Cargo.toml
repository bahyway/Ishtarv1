[package]
name              = "susa-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "BahyWay v4.0 — SusaEngine: the sovereign import threshold. Documents -> EnkiDDB, services+metadata -> EnkiMDB. Stages, analyzes, defers KAKI minting to enkidb-ingest."

# Sovereign law: zero external dependencies. Pure std.
[dependencies]

[lib]
name = "susa_engine"
