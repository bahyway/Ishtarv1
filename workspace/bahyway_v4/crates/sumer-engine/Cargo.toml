[package]
name        = "sumer-engine"
version     = "4.0.0"
edition     = "2021"
description = "BahyWay v4.0 — The Sovereign Sumerian Layer: cuneiform script, Mesopotamian geography, Plimpton mathematics, Vec7DKey."
authors     = ["DUB.SAR 𒁾 Bahaa Fadam"]

# One internal dependency only. No external crates. No async.
# Vec7D + Plimpton anchors come from kinetic-engine (single source).
# KAKI minting is NEVER done here (enkidb-ingest only).
[dependencies]
kinetic-engine = { path = "../kinetic-engine" }

[lib]
name = "sumer_engine"
