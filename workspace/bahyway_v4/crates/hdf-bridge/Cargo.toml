[package]
name              = "hdf-bridge"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "HDF Bridge — translates Hepta spatial AST into DFG execution plans (Hepta→DFG§7)"

[dependencies]
hepta      = { path = "../hepta" }
dfg-engine = { path = "../dfg-engine" }

[dev-dependencies]
hepta      = { path = "../hepta" }
dfg-engine = { path = "../dfg-engine" }
