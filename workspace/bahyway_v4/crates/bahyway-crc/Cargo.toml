[package]
name              = "bahyway-crc"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Native CRC-16/CCITT implementation (no external deps)"

[dependencies]
# No dependencies — pure computation, intentionally minimal

[dev-dependencies]
# No dev deps needed; tests use only core language features
