[package]
name              = "enkisdb"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "EnkiDB Stage Database (EnkiSDB) — landing zone ingestion, Musarû pre-scan, and 15-minute validation sweep (§12.1)"

[dependencies]
bahyway-core      = { path = "../bahyway-core" }
enkidb-kaki       = { path = "../enkidb-kaki" }
enkidb-journal    = { path = "../enkidb-journal" }
enkidw         = { path = "../enkidw" }
musaru-security   = { path = "../musaru-security" }
