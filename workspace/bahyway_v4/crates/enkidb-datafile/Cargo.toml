[package]
name              = "enkidb-datafile"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "ADR-012 Read Node storage: append-only Data Files + a sorted on-disk Index, O(log n) point lookup with no full-file load and no journal replay"

[dependencies]
bahyway-core     = { path = "../bahyway-core" }
enkidb-kaki      = { path = "../enkidb-kaki" }
enkidb-particles = { path = "../enkidb-particles" }
akkvalue         = { path = "../akkvalue" }
