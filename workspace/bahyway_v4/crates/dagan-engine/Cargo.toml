[package]
name              = "dagan-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Sovereign pure-Rust data-analysis station for the BeeMDM ETL chain -- streaming Welford stats, streaming covariance + power-iteration PCA, 7D->3D projection, deterministic farthest-first k-means, Mahalanobis anomaly detection. From the iPhone-session ShamashEngine discussion, renamed: SHAMASH already names something else real in this workspace (Gate 4, dead-particle/zombie judgment). No Pandas/NumPy/Polars -- the whole point, per the original discussion, is that these five algorithms exist natively in pure Rust with zero third-party runtime."

[dependencies]
