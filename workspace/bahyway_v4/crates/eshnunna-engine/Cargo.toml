[package]
name              = "eshnunna-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "EshnunnaEngine -- the sovereign columnar data-file engine (Step 0b). Surrogate u32 -> row offset -> mmap'd column values, replacing journal-walk with direct-offset fetch for HeptaScript value retrieval."

[dependencies]
bahyway-core   = { path = "../bahyway-core" }
enkidb-storage = { path = "../enkidb-storage" }
