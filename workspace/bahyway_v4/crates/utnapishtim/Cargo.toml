[package]
name              = "utnapishtim"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "UTNAPISHTIM -- Sovereign Application Factory: generates a Godot PDM IDE + Three.js web viewer + manifest.akk per client topology"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
