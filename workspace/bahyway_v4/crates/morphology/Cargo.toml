[package]
name              = "morphology"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description       = "Morphological Discovery — the mapping laws (every visual = one sealed quantity) and the honesty coupling (D-1 proposes, D-2 cites) (GL-VIZ-007)."

[dependencies]
