[package]
name              = "bahyway-z3"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Dev-time-only Z3 satisfiability checker for HeptaScript WHERE/WHY clauses (Gate G4 — never linked into any shipped sovereign binary)"

[dependencies]
heptascript = { path = "../heptascript" }
z3          = { version = "0.20", features = ["vendored"] }
