[package]
name              = "ninurta-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "NinurtaEngine — critical-slowing-down / bifurcation detection over particle-collection time series, via the Purussum Calculus (restoring-rate lambda, corroborating indicators, Fourier-surrogate significance)."

[dependencies]
bahyway-core = { path = "../bahyway-core" }
