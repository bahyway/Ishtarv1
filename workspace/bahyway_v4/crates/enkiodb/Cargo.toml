[package]
name              = "enkiodb"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "EnkiDB Operational Database (EnkiODB) — active validated particles promoted from EnkiSDB or committed from EnkiDB GUI transactions (§12.2)"

[dependencies]
bahyway-core    = { path = "../bahyway-core" }
enkidb-kaki     = { path = "../enkidb-kaki" }
enkidb-journal  = { path = "../enkidb-journal" }
enkisdb      = { path = "../enkisdb" }
enkidb-engine    = { path = "../enkidb-engine" }
permanent-storage = { path = "../permanent-storage" }
