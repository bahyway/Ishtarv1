[package]
name              = "akk-loader"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "AKK Loader — compile .akk PARTICLE declarations into Template objects for the BeeMDM ETL chain"

[dependencies]
aaol             = { path = "../aaol" }
template-engine  = { path = "../template-engine" }
