[package]
name = "nusku-score"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "𒀭𒉡𒁻𒆪 Nusku sovereign aggregate scoring — TRS + MedScore + FuzzyScore + FaceScore"

[dependencies]
nusku-engine = { path = "../nusku-engine" }
