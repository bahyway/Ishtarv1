[package]
name = "gula-synthetic-federation"
version = "0.1.0"
edition = "2021"
description = "Deterministic synthetic Baghdad-province pharmaceutical federation for GulaFederationEngine testing."

[workspace]  # standalone crate -- not a member of the workspace/bahyway_v4 monorepo

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
rand_chacha = "0.3"
