[package]
name              = "diagnosis-templates"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "ColorID Diagnosis Templates — quality/freshness/state diagnostics [NEW v4.0]"

[dependencies]
bahyway-core    = { path = "../bahyway-core" }
template-engine = { path = "../template-engine" }
alert-engine    = { path = "../alert-engine" }
