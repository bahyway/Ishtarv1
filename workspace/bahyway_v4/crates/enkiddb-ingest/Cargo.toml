[package]
name        = "enkiddb-ingest"
version     = "0.1.0"
edition     = "2021"
description = "EnkiDDB (Tigris) — upload a documentation folder tree: categorize, chunk into RAG sections, journal, materialize a queryable generation, demo-search it."

[dependencies]
bahyway-core     = { path = "../bahyway-core" }
enkidb-kaki      = { path = "../enkidb-kaki" }
enkiddb          = { path = "../enkiddb" }

[[bin]]
name = "enkiddb-ingest"
path = "src/main.rs"
