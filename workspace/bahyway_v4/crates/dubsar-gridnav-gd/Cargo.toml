[package]
name              = "dubsar-gridnav-gd"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "DubSar Theater grid navigation lens: defect location, Gibil horizon, asset passports, exclusion proof"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
godot      = { version = "0.5", features = ["api-4-3"] }
egd-engine = { path = "../egd-engine" }
fdd-core   = { path = "../fdd-core" }
