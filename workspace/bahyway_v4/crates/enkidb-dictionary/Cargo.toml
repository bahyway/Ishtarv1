[package]
name              = "enkidb-dictionary"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "TĒMĒNU Dictionary — maps attr_name → attr_hash (u32) for the EAV Hepta manifold"

[dependencies]
bahyway-core  = { path = "../bahyway-core" }
bahyway-crc   = { path = "../bahyway-crc" }
