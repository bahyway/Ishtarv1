[package]
name              = "client-dq-profile"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Client-configurable DQ profile — per-attribute nullable/threshold rules that drive the fuzzy scorer"

[dependencies]
bahyway-core   = { path = "../bahyway-core" }
enkidb-journal = { path = "../enkidb-journal" }
fuzzy-engine   = { path = "../fuzzy-engine" }
score-engine   = { path = "../score-engine" }
