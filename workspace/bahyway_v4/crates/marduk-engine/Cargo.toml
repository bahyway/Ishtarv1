[package]
name              = "marduk-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "MardukEngine -- cross-domain Data Particles Modelling Visual Analysis Specialist (BC-MRD-001). Position + Horizon + Topology verbs. Motion/Curvature and the four domain calculi (Sazu/Addu/Suhrim/Namtila) are deliberately not built yet -- see src/lib.rs."

[dependencies]
trend-core  = { path = "../trend-core" }
vgca-engine = { path = "../vgca-engine" }
