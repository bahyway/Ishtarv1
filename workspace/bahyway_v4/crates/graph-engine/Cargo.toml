[package]
name        = "graph-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "𒌷 Graph Algebra — Directed Graph, PageRank, Betweenness Centrality, Strongly Connected Components. ALGEBRA_GLOSSARY.md Part V Layer 6, items 25-28, confirmed absent before this crate. SCC of size >=3 is a mandatory AlertEngine trigger (AML ring detection)."

[dependencies]
alert-engine = { path = "../alert-engine" }
