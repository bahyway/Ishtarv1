[package]
name              = "sla-engine"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "SLA governance engine: fuzzy compliance scoring + stakeholder GUI for BahyWay apps"

[dependencies]
bahyway-core       = { path = "../bahyway-core" }
hepta-sec-policy   = { path = "../hepta-sec-policy" }
hepta-sec-firewall = { path = "../hepta-sec-firewall" }
