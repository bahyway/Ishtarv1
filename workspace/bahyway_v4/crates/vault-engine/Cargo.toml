[package]
name        = "vault-engine"
version     = "0.1.0"
edition     = "2021"
description = "vault-engine — Sovereign file system health analyzer. Walks the OS file tree, detects Gray-Rot (zero-byte, duplicates, orphans, ancient files), classifies collections into BahyWay quality lanes. Zero external dependencies."
authors     = ["DUB.SAR 𒁾"]

# Zero external dependencies — sovereign pure Rust + std only
[dependencies]
