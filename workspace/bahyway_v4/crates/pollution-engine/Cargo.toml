[package]
name        = "pollution-engine"
version     = "0.1.0"
edition     = "2021"
description = "Pollution Engine — Multi-domain (Air/Water/Oil) sensor data ingestion with smart schema auto-detection. Zero external dependencies — sovereign pure Rust."
authors     = ["DUB.SAR 𒁾"]

# Zero external dependencies — sovereign pure Rust
[dependencies]
