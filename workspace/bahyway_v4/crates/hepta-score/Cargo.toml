[package]
name              = "hepta-score"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "Sovereign 7D quality scoring engine — H(P) equation, B11 byte, QualityLane"

[dependencies]
# Pure Rust — zero external dependencies

[dev-dependencies]
# none
