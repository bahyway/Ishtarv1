[package]
name              = "enkimdb-registry"
version.workspace = true
edition.workspace = true
authors.workspace = true
description       = "EnkiMDB Internal Release Profile registry: every engine/crate in this workspace as a queryable artifact. From the iPhone-session 'All Engines Are Particles' discussion (playbook_96_enkimdb design) -- this crate is the concrete, grounded first step: it scans the REAL workspace Cargo.toml files rather than assuming synthetic data, so the registry it builds is always accurate to what actually exists."

[dependencies]
