---
# PB-517 · sila_grid_crate · DRAFT (BahyWay.Ecosystem v4.0 · traffic & HeptaMapSpace arc)
# Running this playbook is the Architect's CSR-08 confirmation.
- name: "PB-517 · sila-grid crate (GL-NAV-002): jam-complex Betti + persistence strip + fundamental diagram"
  hosts: dubsar_workstation
  tasks:
    - name: crate
      ansible.builtin.command:
        cmd: "cargo new --lib sila-grid"
        creates: /home/bfadam/Forge/bahyway_v4/sila-grid/Cargo.toml
    - name: TDA
      ansible.builtin.debug: {msg: "β0 jam islands · β1 gridlock loop (deadlock as a hole) · persistence sees loop forming below threshold"}
    - name: physics
      ansible.builtin.debug: {msg: "Greenshields q(ρ)=vmax·ρ(1−ρ/ρj) · crest law: stay left, shed at gates never at store"}
    - name: journal (NARU)
      ansible.builtin.lineinfile:
        path: /home/bfadam/Forge/bahyway_v4/NARU.journal
        line: "{{ ansible_date_time.iso8601 }} · PB-517 · sila-grid crate scaffolded"
        create: yes
