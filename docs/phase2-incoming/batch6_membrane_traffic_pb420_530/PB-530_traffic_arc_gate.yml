---
# PB-530 · traffic_arc_gate · DRAFT (BahyWay.Ecosystem v4.0 · traffic & HeptaMapSpace arc)
# Running this playbook is the Architect's CSR-08 confirmation.
- name: "PB-530 · traffic & HeptaMapSpace arc gate: assert PB-515..529 sealed in NARU before production wiring"
  hosts: dubsar_workstation
  tasks:
    - name: assert the arc is sealed
      ansible.builtin.shell: "grep -c 'PB-5[12][0-9]' /home/bfadam/Forge/bahyway_v4/NARU.journal"
      register: sealed
      failed_when: sealed.stdout|int < 15
    - name: journal (NARU)
      ansible.builtin.lineinfile:
        path: /home/bfadam/Forge/bahyway_v4/NARU.journal
        line: "{{ ansible_date_time.iso8601 }} · PB-530 · traffic & HeptaMapSpace arc gate passed"
        create: yes
