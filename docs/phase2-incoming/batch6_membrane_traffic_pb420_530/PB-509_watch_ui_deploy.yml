---
# PB-509 · watch_ui_deploy · DRAFT (BahyWay.Ecosystem v4.0 finalization suite)
# Running this playbook is the Architect's CSR-08 confirmation. Nothing is law until run.
- name: "PB-509 · deploy the Watch UI: Šala tablet now, ws://:9100/watch feed reserved for the Theater panel"
  hosts: dubsar_workstation
  tasks:
    - name: deploy tablet
      ansible.builtin.command:
        cmd: "bash -c 'cp /home/bfadam/Forge/bahyway_v4/docs/__DialyWorks/Tu20260811/shala_igigi_watch_v2.html /home/bfadam/Forge/bahyway_v4/shala_vault/'"
    - name: feed contract
      ansible.builtin.debug: {msg: "ws://:9100/watch · {bells, headroom, weir, decay, sentinels} v1 — consumers adapt to it, never it to them"}
    - name: journal (NARU discipline)
      ansible.builtin.lineinfile:
        path: /home/bfadam/Forge/bahyway_v4/NARU.journal
        line: "{{ ansible_date_time.iso8601 }} · PB-509 · Watch UI deployed · feed contract reserved"
        create: yes
