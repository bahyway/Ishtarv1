---
# PB-432 · dashboard_census_service · DRAFT (BahyWay.Ecosystem v4.0 finalization suite)
# Running this playbook is the Architect's CSR-08 confirmation. Nothing is law until run.
- name: "PB-432 · ecosystem-dashboard census endpoint: live residence counts for the Šala/Theater dashboard"
  hosts: enkidb_node_write
  tasks:
    - name: service scaffold
      ansible.builtin.command:
        cmd: "cargo new --bin bahyway-censusd"
        creates: /home/bfadam/Forge/bahyway_v4/bahyway-censusd/Cargo.toml
    - name: endpoints
      ansible.builtin.debug: {msg: "/census → {processed, golden, warehouse, custody(with causes), inflight, docs} · read-only, ENLIL-backed"}
    - name: journal (NARU discipline)
      ansible.builtin.lineinfile:
        path: /home/bfadam/Forge/bahyway_v4/NARU.journal
        line: "{{ ansible_date_time.iso8601 }} · PB-432 · censusd scaffolded"
        create: yes
