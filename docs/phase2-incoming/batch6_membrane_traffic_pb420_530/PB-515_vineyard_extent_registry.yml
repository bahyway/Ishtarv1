---
# PB-515 · vineyard_extent_registry · DRAFT (BahyWay.Ecosystem v4.0 · traffic & HeptaMapSpace arc)
# Running this playbook is the Architect's CSR-08 confirmation.
- name: "PB-515 · extent registry + soil telemetry per file-structure row (stem-locality diagnosis)"
  hosts: enkidb_node_write
  tasks:
    - name: crate
      ansible.builtin.command:
        cmd: "cargo new --lib karanu-extents"
        creates: /home/bfadam/Forge/bahyway_v4/karanu-extents/Cargo.toml
    - name: stem-locality
      ansible.builtin.debug: {msg: "locality = 1 − fragments/berries · KISPU columnar contiguity makes a lineage one stem"}
    - name: soil
      ansible.builtin.debug: {msg: "NVMe wear/temp/write-amp per row · phylloxera = stem-disease ∧ soil.exhausted → migrate extents"}
    - name: journal (NARU)
      ansible.builtin.lineinfile:
        path: /home/bfadam/Forge/bahyway_v4/NARU.journal
        line: "{{ ansible_date_time.iso8601 }} · PB-515 · vineyard extent registry scaffolded"
        create: yes
