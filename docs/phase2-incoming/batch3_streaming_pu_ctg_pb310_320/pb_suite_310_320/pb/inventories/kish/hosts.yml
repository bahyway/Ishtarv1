# KISH stream — conservative, sealed, boring on purpose (GL-OPS-002 K-1)
all:
  children:
    host_forge:      { hosts: { localhost: { ansible_connection: local } } }
    write_node:      { hosts: { enki-write: { ansible_host: 192.168.122.101 } } }
    read_node:       { hosts: { enki-read:  { ansible_host: 192.168.122.107 } } }
    vault_librarian: { hosts: { vault-lib:  { ansible_host: 192.168.122.120 } } }
    vault_body:      { hosts: { nas-vault:  { ansible_host: 192.168.122.200 } } }
