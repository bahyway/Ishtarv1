# EnkiDDB team-authorship allowlist (PB-198).
#
# One git commit-author email per line. A file is only authorized to
# ingest through enkiddb-cli / enkiddb-write-server's INGEST_DIR:<path>
# if its last git commit author's email appears here (case-insensitive).
# Fails closed -- an email NOT listed here is rejected, as is any file
# with no determinable git author at all.
#
# Bind-mounted read-only into the enkiddb-write container at
# /source/config/enkiddb_team_allowlist.txt (see playbook_192's
# ENKIDDB_TEAM_ALLOWLIST env var). Edit this file and restart the
# container to change who's trusted -- no rebuild needed.
#
# Seeded from this repo's own real git history
# (crates/enkiddb/src/authorship.rs::TeamAllowlist::seed) plus the
# Architect's known address.
#
# noreply@anthropic.com (this agent's own commit identity) is included
# below by the Architect's explicit, standing decision (2026-07-19,
# session claude/iphone-playbooks-crosstribe-eval-nbc5sw) -- every commit
# under this identity in this repo has been made at the Architect's
# direct instruction throughout the whole session, and the Architect
# chose to unblock full-corpus ingestion rather than re-author the
# existing docs/ tree first. This is a deliberate, non-default opt-in,
# not the library's own seed default (crates/enkiddb/src/authorship.rs's
# TeamAllowlist::seed still excludes it) -- it only applies where this
# file is explicitly loaded via --team-allowlist / $ENKIDDB_TEAM_ALLOWLIST.
bahaa.fadam@gmail.com
bfadam@bahyway.com
bfadam@bahyway.example
122043936+bahyway@users.noreply.github.com
noreply@anthropic.com

# bahyway@enkidb.io -- eriduous-vdi's own default git identity (found
# live 2026-07-26: 9 files committed directly on that machine used this
# email, not bfadam@bahyway.com, causing a real PB-208 rejection despite
# being the Architect's own real infrastructure). Same person, same
# machine, just a different configured git user.email than the
# workstation identity above.
bahyway@enkidb.io
